# DOCKER DEPLOYMENT .env.docker settings
# Users will copy this to .env.docker and customize.

# IMPORTANT: Set this to an ABSOLUTE path on your HOST machine where Phentrieve data will live.
# This directory will be mounted into Docker containers.
# Example (Linux/macOS): PHENTRIEVE_HOST_DATA_DIR=/srv/phentrieve/data
# Example (Windows for WSL/Docker Desktop): PHENTRIEVE_HOST_DATA_DIR=/c/phentrieve_data
PHENTRIEVE_HOST_DATA_DIR=

# Publicly accessible URLs that the frontend needs to know
# These should point to the domains configured in Nginx Proxy Manager
VITE_API_URL_PUBLIC=https://phentrieve-api.kidney-genetics.org/api/v1
VITE_FRONTEND_URL_PUBLIC=https://phentrieve.kidney-genetics.org

# Default model for setup script to check/build index for, if missing
DEFAULT_SETUP_INDEX_MODEL=FremyCompany/BioLORD-2023-M

# Logging levels for services
LOG_LEVEL_API=INFO

# Shared network with Nginx Proxy Manager (verify the name in your NPM setup)
NPM_SHARED_NETWORK_NAME=nginxproxymanager_default
