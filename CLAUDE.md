# CLAUDE.md

@AGENTS.md

Claude Code entrypoint only:

- Use `.planning/README.md` as the planning index.
- Keep shared repo guidance in `AGENTS.md`, not here.
- Add Claude-specific scoped rules under `.claude/` only when they are truly
  tool-specific.
